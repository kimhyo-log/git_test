# Git Branch Strategy Test

This repository is a local sandbox for testing the promotion flow:

DEV -> QA -> STG -> PROD

Feature branches use the `ISR-0000` naming pattern and are squash-merged into `DEV`.
